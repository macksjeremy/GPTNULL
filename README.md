To run the adversarial attack, navigate to the gptzero folder and use the following command:

```textattack attack --recipe textfooler --model-from-file model.py --dataset-from-file gendataset.py --num-examples 100```